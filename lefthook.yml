commit-msg:
    scripts:
        "verify-commit-msg":
            runner: bash

pre-commit:
    commands:
        check:
            glob: "{*.sh}"
            run: shellcheck {staged_files} --severity=warning
        format:
            glob: "{*.sh,*.inc}"
            run: shfmt -w {staged_files}
