pre-commit:
    scripts:
        "verify-commit-msg":
            runner: bash
    commands:
        format:
            glob: "*.{sh,inc}"
            run: shfmt -w {staged_files}
        check:
            glob: "*.{sh}"
            run: shellcheck {staged_files}
